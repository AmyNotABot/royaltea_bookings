import React from 'react'
import ReactDOM from 'react-dom/client'
import TestTestTest from './bricks/example'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TestTestTest/>
  </React.StrictMode>,
)
